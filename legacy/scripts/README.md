# Historical Script Versions

The former `IF_Quant_Pipeline_TEST.groovy` was an ignored, non-authoritative
local development copy and is not promoted as executable source in the organized
repository.

Use the complete pre-organization snapshot instead:

```powershell
git show codex/legacy-pre-reorganization:IF_Quant_Pipeline.groovy
```

Earlier production versions are also available through `git log --
IF_Quant_Pipeline.groovy`. The active executable remains
`../../IF_Quant_Pipeline.groovy`.
