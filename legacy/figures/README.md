# Archived Figures

These figures document earlier pipeline stages and are retained for historical
comparison only:

- `if_quant_cell_counting_backbone.png`: earlier cell-counting backbone.
- `if_quant_per_marker_positivity_rules.png`: earlier role-specific positivity
  diagram in which intensity remained too prominent.
- `overlay_analysis.png`: earlier QC/overlay development image.

The current hierarchy is maintained as Mermaid in `../../WORKFLOW.md` and
`../../docs/MARKER_MORPHOLOGY_GUIDE.md` so it stays synchronized with the code.
